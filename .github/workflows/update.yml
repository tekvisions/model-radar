name: Update Model Radar
on:
  schedule:
    - cron: "52 9 * * *"
  workflow_dispatch: {}
permissions:
  contents: write
# Commit uses default GITHUB_TOKEN (no workflow retrigger). Do not add a push: trigger.
concurrency:
  group: update
  cancel-in-progress: false
jobs:
  refresh:
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_PROJECT: model-radar
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: Rebuild from the Hugging Face API
        run: python build_data.py
      - name: Commit data if changed
        run: |
          git config user.name "model-radar-bot"
          git config user.email "actions@users.noreply.github.com"
          if ! git diff --quiet -- data.json; then
            git add data.json && git commit -m "data: daily trending refresh ($(date -u +%Y-%m-%d))" && git push
          else echo "No data change."; fi
      - name: Deploy to Vercel
        if: ${{ env.VERCEL_TOKEN != '' }}
        run: python deploy.py
